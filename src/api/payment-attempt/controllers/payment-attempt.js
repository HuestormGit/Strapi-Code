'use strict';

/**
 * payment-attempt controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::payment-attempt.payment-attempt');
